/**
 * Created by Andrew on 2/6/2017.
 */
const initialState = {
    isLoading: true,
};
let homeReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default homeReducer;